command:
  - uvicorn
  - backend.main:app
  - --host
  - 0.0.0.0
  - --port
  - 8000

env:
  - name: DATABRICKS_SQL_WAREHOUSE_HTTP_PATH
    value: /sql/1.0/warehouses/REPLACE_WITH_YOUR_WAREHOUSE_ID
  - name: DATABRICKS_CATALOG
    value: main
  - name: DATABRICKS_SCHEMA
    value: data_collector
